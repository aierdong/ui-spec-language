# Requirement — Registration

Create a registration page where a new user can create an account with email and password, agree to the terms, and submit the form. The page should show progress while creating the account, show an error if registration fails, and navigate to the welcome page when the account is created successfully.

The spec must stay semantic: do not use component names, CSS layout instructions, framework names, or HTML concepts.
