# Requirement — File Upload

Create a file upload page where a user selects a document, submits it for upload, sees upload progress, and receives success or failure feedback. The file must be validated by required, size, and type constraints. Successful upload should navigate to the uploaded file detail page.

The spec must stay semantic: do not use component names, CSS layout instructions, framework names, or HTML concepts.
