# Requirement — Account Recovery

Create an account recovery flow for users who cannot access their account. The user enters an email address, submits the recovery request, then provides an OTP if required. The flow should communicate that recovery instructions were sent, show invalid OTP errors, and navigate back to sign in after recovery succeeds.

The spec must stay semantic: do not use component names, CSS layout instructions, framework names, or HTML concepts.
